import streamlit as st
 
st.title("나의 첫 웹앱")
st.write("안녕하세요! 반갑습니다.")
지역 = st.selectbox("지역을 골라 보세요", ["서울", "양평", "부산"])
st.write("당신이 고른 지역:", 지역)
숫자 = st.slider("좋아하는 숫자", 0, 100)
st.write("고른 숫자:", 숫자)
 
if st.button("풍선 날리기"):
	st.balloons()
import streamlit as st
 
tab1, tab2 = st.tabs(["첫 번째 탭", "두 번째 탭"])
 
with tab1:
	st.write("여기는 첫 번째 탭이에요.")
 
with tab2:
	숫자 = st.slider("숫자를 골라보세요", 0, 100)
	st.write("고른 숫자:", 숫자)
서울과 양평의 기온 데이터를 비교해서 도시 열섬현상을 살펴보는
streamlit 웹앱을 만들려고 해. 아래 내용을 참고해서 만들어줘.
 
1. 데이터: 같은 폴더에 있는 "서울_기온.csv"와 "양평_기온.csv" 두 파일
2. 두 파일 모두 열 이름은 지점, 지점명, 일시, 기온(°C) 이고,
   일시는 "2025-01-01 01:00" 같은 시간별 데이터야. (2025년 1년치)
3. 한글 파일이라 pandas로 읽을 때 encoding="cp949"를 꼭 써줘.
4. 그래프는 이런 걸 보여주면 좋겠어:
   ① 1년간 두 지역의 기온 변화 (선그래프)
   ② 시각(0~23시)별 평균 기온차, 서울-양평 (막대그래프)
   ③ 월(1~12월)별 평균 기온차, 서울-양평 (막대그래프)
5. 라이브러리는 streamlit과 pandas만 사용해줘.

